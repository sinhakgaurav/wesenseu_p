[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    integration: hits live API (default base http://localhost:8000)
filterwarnings =
    ignore::DeprecationWarning
